flask
yt-dlp
python-magic
Pillow
mutagen
requests
packaging
# pymumble 2.0 is vendored (see vendor/README.md) so builds never depend on
# reaching codeberg.org - the NAS network cannot clone from it reliably.
./vendor/pymumble
pyradios
spotdl